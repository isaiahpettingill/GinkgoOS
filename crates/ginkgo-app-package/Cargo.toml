[package]
name = "ginkgo-app-package"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true

[features]
default = []
host = []

[dependencies]
hmac-sha256 = { version = "1.1.14", default-features = false }

[lib]
path = "src/lib.rs"
